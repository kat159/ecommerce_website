import React from 'react'

/**
 * Used for CRUD on foreign table in many to many relationship
 *  Selective
 *  add new record in join table when added 
 *  CANNOT change the value of the field
 *  delete the join table record in database when deleted
 */

export default function MyCRUDTagSelector() {
  return (
    <div>MyCRUDTagSelector</div>
  )
}
